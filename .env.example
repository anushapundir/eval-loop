# Copy to .env and fill in. The real .env is gitignored.

# Required only when MODEL_PROVIDER routes to Haiku, and always for the LLM judge.
ANTHROPIC_API_KEY=sk-ant-...

# Default agent runtime: "ollama" (free, local) or "haiku" (paid, all-Haiku mode).
MODEL_PROVIDER=ollama
